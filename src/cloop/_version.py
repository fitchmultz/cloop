"""Version metadata for the Cloop package.

Purpose:
    Provide a runtime version constant that matches pyproject.toml metadata.

Responsibilities:
    - Expose `__version__` for API metadata and CLI version output.

Non-scope:
    - Automatic version generation from git state.
    - Release tagging or changelog management.
"""

__version__ = "0.1.0"
